# SPDX-FileCopyrightText: 2026 KDE Agents Usage contributors
# SPDX-License-Identifier: MIT

name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Get the source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.10"

      - name: Run unit tests
        run: python3 -m unittest discover -s tests -v

      - name: Compile Python files
        run: python3 -m py_compile package/contents/code/agent-usage-json package/contents/code/claude-statusline-collector

      - name: Examine the installer syntax
        run: bash -n install.sh

      - name: Validate package metadata
        run: python3 -m json.tool package/metadata.json >/dev/null
